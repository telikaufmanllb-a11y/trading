# Pinned for reproducibility (HARD RULE 9). Versions are install-verified at the start of
# Stage 1; if a pin is unavailable in the runtime, update it and log the substitution in
# DECISIONS.md. Do NOT loosen pins to "latest" — reproducible results require fixed versions.

# Core data
# numpy pinned to the 1.x line: quantstats 0.0.64 requires numpy<2.0.0 (see DECISIONS.md D-0006).
numpy==1.26.4
pandas==2.2.3
python-dateutil==2.9.0.post0

# EDGAR ingestion + Form 4 XML parsing
requests==2.32.3
lxml==5.3.0

# Point-in-time storage
pyarrow==18.1.0
duckdb==1.1.3

# Analysis / tearsheets
matplotlib==3.10.0
quantstats==0.0.64

# Tests
pytest==8.3.4
